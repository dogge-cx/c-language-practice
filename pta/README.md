# pta/

PTA 基础编程题目集刷题专用目录。

- 每个题一个 `.c` 文件，文件名带题号，如 `pta_1001.c`
- 文件开头用注释写清：题号、题目大意、考察点
- 例：

```c
// pta_1001.c
// 考察：scanf 读 double 用 %lf，递推求和
#include <stdio.h>
int main() { /* ... */ return 0; }
```
